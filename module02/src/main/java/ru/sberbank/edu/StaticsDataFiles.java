package ru.sberbank.edu;

public class StaticsDataFiles extends StatisticsData{

    /**
     * храним и работаем со статистикой, которую используем дальше для записи
     * @param countLines
     * @param spaceCount
     * @param biggestLine
     */
    public StaticsDataFiles(int countLines, int spaceCount, String biggestLine) {
        super(countLines, spaceCount, biggestLine);
    }
}

